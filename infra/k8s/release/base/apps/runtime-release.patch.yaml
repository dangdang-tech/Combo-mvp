apiVersion: apps/v1
kind: Deployment
metadata:
  name: runtime
spec:
  template:
    metadata:
      annotations:
        combo.build/release-contract: v1
    spec:
      containers:
        - name: runtime
          envFrom:
            - configMapRef:
                name: combo-release

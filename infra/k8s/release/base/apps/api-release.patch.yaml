apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  template:
    metadata:
      annotations:
        combo.build/release-contract: v1
    spec:
      containers:
        - name: api
          envFrom:
            - secretRef:
                name: combo-env
            - configMapRef:
                name: combo-release

apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
spec:
  template:
    metadata:
      annotations:
        combo.build/release-contract: v1
    spec:
      containers:
        - name: worker
          envFrom:
            - configMapRef:
                name: combo-release

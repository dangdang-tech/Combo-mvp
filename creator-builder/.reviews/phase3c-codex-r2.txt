5 项复审：1 PASS，2 PASS，3 PASS，4 FAIL，5 PASS。

**阻塞项**

- P1 [extract-repo.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/extract/extract-repo.ts:217): `insertEvidenceProtected()` 返回 `false`、`updateCandidateSegmentCountProtected()` 返回 `false` 都没触发 rollback；retry 成功路径同样忽略证据 insert / count update 的 0 行。会提交 ready 候选但证据或 `segment_count` 半残。修法：事务开头拿 `jobs ... FOR UPDATE` guard，任一受保护写 0 行直接 rollback；`segment_count` 从实际 evidence count 回填。

- P1 [extract.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/handlers/extract.ts:667): `reportRetryFailed()` 吞掉 failed 回写失败，仍 `appendItem(failed)` 并 finalize completed。DB 可能还停在 `generating`，流里却显示 failed。修法：检查 `applyRetryFailureProtected` 返回值；0 行按 fenced_out 退出，DB 异常上抛，不得 append / finalize。

- P1 [create-extract-job.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/extract/create-extract-job.ts:253): 候选已 `generating` 时用新 `Idempotency-Key` 重试会落到 `not_found`，路由返回 404；契约要求重试在途为 `423 RESOURCE_LOCKED`。修法：`CreateRetryJobResult` 增加 `locked`，status=`generating` 返回 locked，route 回 423 + `action:'wait'`。

- P1 [create-extract-job.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/extract/create-extract-job.ts:225): retry job 初始 `jobs.progress.items=[]`，而 [routes/_sse.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/routes/_sse.ts:130) 的首帧只读 `jobs.progress`，所以 retry 新流的 `state_snapshot` 不含该候选 `generating` 态。修法：受理 retry 时把 `{id, status:'generating', isNew:false, name}` 写入 retry job 初始 progress.items。

VERDICT: FAIL
无 P0/P1 阻塞，可作为 STEP① 导入后端基线。

r6 三项复核通过：
- 直传 CTE 已收敛为 active `FOR UPDATE` → `INSERT FROM active` → 单次 `UPDATE consumed_at + job_id`，父行只改一次；见 [upload-manifest-repo.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/import/upload-manifest-repo.ts:192)。
- 直传恢复已在桶完整性校验前短路，`consumed_at && job_id` 直接恢复 `JobView`，不 list 桶；见 [import-handlers.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/routes/import-handlers.ts:145)。
- 助手恢复已在 `used_at`/终态拒绝前短路，但仍先校验正确码；`Authorization` 已纳入 upload-scope idempotency hash；见 [pair-auth.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/middleware/pair-auth.ts:109) 和 [idempotency.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/middleware/idempotency.ts:51)。

非阻塞建议：
- P2: 更新 `20-step1-import.md`，当前仍写着 presign “不写库、无副作用”，但实现已持久化 `import_uploads` manifest。
- P2: 后续有 PG 环境时补 DB CHECK：`consumed_at IS NULL OR job_id IS NOT NULL`，以及 `phase <> 'job_created' OR job_id IS NOT NULL`。
- P2: 补一个全路由级助手终态重试用例，覆盖 `pair-auth` + `idempotency` 的真实 preHandler 顺序。

VERDICT: PASS
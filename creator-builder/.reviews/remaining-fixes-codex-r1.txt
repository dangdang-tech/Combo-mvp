VERDICT: FAIL

阻塞项：

`[中]` [creator-builder/apps/api/src/structure/create-capability.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/structure/create-capability.ts:60) — `isCapabilitySlugConflict()` 在 `constraint === '' && message === ''` 时把所有 PG `23505` 都映射成 slug 冲突 409。  
这不满足“精确识别 uq_capabilities_slug、不误吞其它约束”的要求；同事务还可能触发 `capabilities.id`、`capability_versions.id`、`uq_capability_version`、`uq_capability_versions_capability_id` 等唯一约束。  
修法：删掉空 constraint/message 兜底，只接受精确约束名或 message 中包含 `uq_capabilities_slug`：

```ts
return constraint === 'uq_capabilities_slug' || /\buq_capabilities_slug\b/.test(message);
```

并补负例测试：`{ code: '23505', constraint: 'uq_capability_version' }` 和 `{ code: '23505' }` 都不能变 409，应继续走原 DB 异常路径。

核验摘要：

SSE 修复整体过关：active job 原路径被 `jobId && isActive` 保留，connect-before-job 会挂 `awaitActivation`，关闭时通过 `AbortController` 清心跳/订阅/轮询；终态接管的第二个兜底 `done completed` 会被 `push()` 的 stopped guard 吞掉，不会实发双 done。鉴权仍走 `requireSseAuth()`，只认 Cookie。诚实评估：无全局上限/退避，每个等待连接每 1s 查一次 DB，非阻塞但需要容量预期。

LLM 默认 slug：`anthropic/claude-sonnet-4.6` 当前在 OpenRouter models API 中存在；Anthropic provider 仍走不带 `anthropic/` 的 `DEFAULT_MODEL`。价格表只影响 audit 估算，不是计费真源。来源：[OpenRouter models API](https://openrouter.ai/api/v1/models)

worker 日志：对外 ErrorEnvelope 仍由 `normalizeToErrorBody()` 重建，无 code/stack；内部日志含 `code` 和原始 `err`。价格显示实现未改，只加锁定测试，单位仍是 CNY micros。

验证限制：目标 vitest 命令在只读沙箱失败，Vite 试图写 `vitest.config.ts.timestamp-*.mjs` 被 EPERM 拒绝；不是测试断言失败。_changed files_ 扫描未发现 `any`。
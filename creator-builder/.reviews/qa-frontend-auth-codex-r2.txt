VERDICT: FAIL

[P1] [creator-builder/apps/web/src/shell/auth.tsx](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/web/src/shell/auth.tsx:68): `fetchMe()` 把 200 响应当裸 `MeView` 解析，但真实后端 `/api/v1/me` 返回的是 `Envelope<MeView>`，即 `{ data: me, meta }`，见 [auth-handlers.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/routes/auth-handlers.ts:348)。结果是真实已登录用户会被 `MeViewSchema.parse(body)` 判为解析失败，降级成 `error`，`RequireAuth` 永远不放行受保护路由。新增测试也用裸 `ME` mock 了 200，掩盖了这个回归。

修法：`fetchMe()` 应解析标准包络，例如 `MeViewSchema.parse((body as { data?: unknown }).data)`，或定义 `z.object({ data: MeViewSchema })` 校验；`auth.test.tsx` 的 200 用例必须改成 `{ data: ME, meta: ... }`，并断言登录态能进入 `<Outlet/>`。

逐条确认上一轮 3 点：

1. `/creators/:creatorId/profile` 已移到 `PublicLayout` 公开组，`/profile` 仍在 `RequireAuth -> ProtectedLayout -> Shell` 下。ProfilePage 不依赖 Shell/Account 上下文，公开裸渲染不会因此崩。此项修复成立。

2. `fetchMe` 的 401/非 401 分流方向基本对：只把 401 判 anon，403/500/503/网络判 error；UI 文案是人话，没有裸露 status/code；retry 是 `q.refetch()`，不是去登录；AbortError 透传；解析失败不伪造 authed。但由于 200 包络解析错误，真实 authed 路径被打断，所以此项未真正修复。

3. `returnTo` 覆盖了守卫闸门、dashboard/profile 的 escalate CTA，取值都是 `window.location.pathname + window.location.search`，并经 `encodeURIComponent`。当前调用点只传站内相对路径。此项修复成立。非阻塞 nit：`loginUrl(returnTo)` 自身不拒绝绝对 URL，后续若开放给更多调用方，最好在 helper 内也防一层。

三铁律 / BUG-001..007：

- “绝不裸露错误码”：本轮相关路径未见裸 status/code 泄漏。
- “永不裸转圈”：auth guard 有有限 loading/error 态，QueryClient retry 策略未见冲突。
- “已生成内容不丢”：未登录 `/create/*` 会在守卫前被拦，向导不挂载，未见自动 POST 草稿回归。
- BUG-001/003/004/005/006/007 路由层修复方向成立；BUG-002 的匿名边界成立，但因 `/me` 200 包络解析回归，登录后核心受保护链路仍不可用。

未运行测试；本次为只读复审，结论基于实际源码和后端 handler 对照。
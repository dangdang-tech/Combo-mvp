r4 两项核验：已落地自洽。

配对建 job：`active SELECT ... FOR UPDATE`、`INSERT ... SELECT FROM active`、末尾单次 `UPDATE import_pairings` 同时写 `phase/used_at/job_id` 已在 [pairings-repo.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/import/pairings-repo.ts:362) 落地；expired / used / attempts 耗尽不建 job 有回归覆盖。

完成事务：`lockRunningJobInTx` 先 `FOR UPDATE` 锁 running job，`!completed` 抛 `FinalizeFencedOut` 触发 rollback，外层吞哨兵、真异常上抛重试，已在 [jobs/handlers/import.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/handlers/import.ts:75) 和 [jobs/handlers/import.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/handlers/import.ts:411) 落地；supersede 后 complete 前 fence-out 回滚有测试覆盖。

阻塞项：

[P1] [import-handlers.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/routes/import-handlers.ts:184) — 直传路径先 `consumeUploadManifest`，再 `createAndEnqueueImportJob`。若 `queue.enqueue` 失败，[create-job.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/import/create-job.ts:109) 会删除/失败刚建 job，handler 返回 503 retriable，但 manifest 已 `consumed_at`，同一 `uploadId` 重试会走 [import-handlers.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/routes/import-handlers.ts:193) 返回 404 失效，无法按 503 语义重试。修法：把 manifest consume + job insert 放同一事务，并在 enqueue 失败时保留 queued job 交给 `staleQueued` 补投；或返回 503 时同事务回滚 `consumed_at` 与 job。补测试：enqueue 失败后同一 uploadId 可重试成功或可恢复同一 job。

未跑测试：本轮只读，且 git/xcrun 已因沙箱无法写 `/tmp` 缓存报错。

VERDICT: FAIL
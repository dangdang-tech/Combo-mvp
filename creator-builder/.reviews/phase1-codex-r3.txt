阻塞项：

1. `creator-builder/docs/contracts/20-step1-import.md:95`、`:205`、`:296`、`:727` 仍把错误人话列/错误字段写成 `message`，而 `_index.md:7` 声称“全目录无残留”。  
修法：全部改为 `userMessage` 口径；文案类“端点 message/错误 message”也改成“错误 userMessage/可见文案”，避免继续作为同义字段。

2. `50-step5-publish.md:57` 要求被拒重发“从 rejected version 派生新 draft”，但 `40-step3-4-structure.md:350-356`、`:374-376` 的建新版本入口只支持 `sourceCandidateId` 或 `capabilityId`，且 `capabilityId` 分支要求当前版是 `published`。首发被拒无上一版时无路可走；有上一版时也会从上一 published 版派生，而不是从被拒版派生。  
修法：40 域 `POST /capabilities` 增加 `fromVersionId/baseVersionId`，允许从本人 `review_rejected` 版复制 manifest 派生新 `draft`；同步 50 的重试/编辑入口和错误表。

3. `10-auth-logto.md:56`、`:200`、`:459` 说 B-21 `connect/upload` 走“配对码换短时 Bearer token / Bearer <jwt> / JWT 校验链路”，但 `20-step1-import.md:161`、`:166`、`:504` 说上传用 `Authorization: Bearer <pairingCode>` + 表单 `pairId`，走 pairing_code_hash 校验。  
修法二选一：要么 10 改为 B-21 使用独立 `PairAuth(pairId+pairingCode)`，不进 JWT 中间件；要么 20 增加明确 token exchange 端点，并把失败计数绑定在 exchange 阶段。

4. `50-step5-publish.md:588-633` 的批量发布 fence CTE 有模板，但计数模板 B 无“该 item 是否刚从非终态进入终态”的防重条件；同一 item 终态回写被重复执行时会重复递增 `published_count/failed_count`，破坏“全部发布不漏不重”。  
修法：把 item 终态迁移与 batch 计数合成单条 CTE，`UPDATE publish_batch_items ... WHERE state NOT IN ('published','failed') RETURNING` 后再按返回行递增；或从 items 聚合重算 batch counters。

VERDICT: FAIL
只读评审结论：`_consistency-report.md` 的“无阻塞”结论不成立。契约当前不能作为实现基线冻结。

**P0 阻塞项**

1. 【creator-builder/docs/contracts/70-events-infra.md:108-117】outbox `xid` 水位算法会跳过低序号未提交事件：SQL 先过滤 `xid < xmin`，就永远遇不到“首条 unsafe xid”，消费者仍会推进 cursor，导致事件永久漏消费。  
【可操作修法】按 `seq > cursor ORDER BY seq` 取连续批次，不在 SQL 过滤 `xid`；应用层遇到第一条 `xid >= xmin` 立即停止，只提交已处理的连续安全前缀。

2. 【creator-builder/docs/contracts/30-step2-extract.md:469-478】`candidate_evidence` 没有 schema 级血缘约束，DB 允许 candidate 属于 snapshot A、segment 属于 snapshot B、evidence.snapshot_id 伪填任意值。  
【可操作修法】给 `capability_candidates(id,snapshot_id)` 与 `session_segments(id,snapshot_id)` 加唯一约束，再用两个复合 FK 约束 `candidate_evidence(candidate_id,snapshot_id)` 和 `(segment_id,snapshot_id)`。

3. 【creator-builder/docs/contracts/00-约定与状态机.md:326】【creator-builder/docs/contracts/20-step1-import.md:503-506】fence 租约只停留在前置检查示例，实际落库表缺少可被 `WHERE job_id AND fence_token` 保护的写入闭环，旧 worker 可能检查通过后再写入。  
【可操作修法】所有 worker 写入改成单条事务 CTE：`INSERT/UPDATE ... SELECT ... FROM jobs WHERE id=? AND fence_token=? AND status='running'`；必要时补 `job_id/fence_token` 血缘列。

4. 【creator-builder/docs/contracts/30-step2-extract.md:203-211】【creator-builder/docs/contracts/00-约定与状态机.md:270-271】单项候选重试被设计成回到“原 extract job SSE”，但原 job 已发送 `done` 后客户端会关闭流；重试结果无可靠投递路径。  
【可操作修法】候选重试必须创建新 retry job 与新 `eventsUrl`，或定义 candidate 级事件流；不得在已 terminal 的 job stream 后继续追加业务结果。

5. 【creator-builder/docs/contracts/40-step3-4-structure.md:407-409】【creator-builder/docs/contracts/10-auth-logto.md:270-277】结构化 SSE 鉴权与全局 Logto 契约冲突：一处允许 query/header token 且用 SSE error 表达鉴权失败，另一处明确 Cookie only、失败走 HTTP ErrorEnvelope。  
【可操作修法】统一为同源 Cookie 或明确的 Authorization header，禁止 query token；鉴权/权限失败在建流前返回 HTTP 错误信封。

6. 【creator-builder/docs/contracts/40-step3-4-structure.md:545-548】【creator-builder/docs/contracts/50-step5-publish.md:426-452】`current_version_id` / `publication.current_version_id` / `listing.version_id` 都只 FK 到“任意 version”，不保证属于同一个 capability。  
【可操作修法】给 `capability_versions(capability_id,id)` 加唯一约束，并把上述引用全部改为复合 FK。

7. 【creator-builder/docs/contracts/50-step5-publish.md:374-380】批量发布 SSE 用 `published/total` 表示完成度；只要有失败项，最终进度永远不到 `total`，违反“批量逐项无连坐”和“永不裸转圈”。  
【可操作修法】拆成 `processedCount = published + failed`、`publishedCount`、`failedCount`，完成态以 processed/total 判定。

8. 【creator-builder/docs/contracts/50-step5-publish.md:32-38】【creator-builder/docs/contracts/50-step5-publish.md:342-348】评审拒绝状态机自相矛盾：状态表说有上一版时拒绝的新版本应回退/下架，事务说明却把 current version 标成 `review_rejected`。  
【可操作修法】明确“被拒版本”和“当前对外版本”两条状态线；publication 回指上一 published version，被拒版本单独记录 reject_reason/rejected_at。

9. 【creator-builder/docs/contracts/40-step3-4-structure.md:62-73】【creator-builder/docs/contracts/40-step3-4-structure.md:256-270】`field_stuck` / `STRUCTURE_FIELD_FAILED` 示例把硬字段 `output` 当作可失败软字段；验收要求硬字段锁定、不生成、不报字段级生成错误。  
【可操作修法】字段级事件 schema 限定为 `SoftFieldKey`，示例改为 `instructions` / `tagline` 等软字段。

10. 【creator-builder/docs/contracts/40-step3-4-structure.md:20-27】【docs/开工总纲-创作者中心主链路.md:167-183】STEP③ 写成“纯前端、不写库、不产生草稿”，但权威 PRD 要求每步都能保存草稿并从工作台精确续传。  
【可操作修法】区分“选择变化不自动写库”和“保存草稿/下一步提交会持久化 selection”，补草稿保存接口与恢复 schema。

11. 【creator-builder/docs/contracts/00-约定与状态机.md:119-125】【creator-builder/docs/contracts/10-auth-logto.md:126-135】错误信封和登录回调把内部 `error.code` 直接放入响应/URL，契约没有禁止 UI 裸展示，和三条硬规则冲突。  
【可操作修法】API 可保留机器码但必须定义 `userMessage/action` 为唯一可展示字段；登录失败 URL 用 opaque failure id，不传内部错误码。

12. 【creator-builder/docs/contracts/70-events-infra.md:169-186】`dead_events` 没有 FK 到 `outbox_events(event_id)`，也没有 `status` / `attempts` CHECK，schema 层不成立。  
【可操作修法】加 `FOREIGN KEY(event_id)`、`CHECK status IN (...)`、`CHECK attempts >= 0`，唯一键按 consumer/topic/event 语义收紧。

13. 【creator-builder/docs/contracts/70-events-infra.md:513-527】【creator-builder/docs/contracts/70-events-infra.md:567-599】冻结表声明 FK 闭合但实际缺 FK：usage/runtime/experience pack 多处只写 UUID，无引用约束。  
【可操作修法】用后置 `ALTER TABLE` 补齐到 users/capabilities/capability_versions/runtime_sessions/session_segments 的 FK，或明确标成 intentional loose，不得自称闭合。

14. 【creator-builder/docs/contracts/20-step1-import.md:181-188】`ConnectUploadResult` 在 `status:'uploading'` 时仍要求 `jobId`，但上传未完成阶段通常还没有云端解析 job。  
【可操作修法】改成判别联合：`uploading` 不含 `jobId`，`job_created` 必含 `jobId/eventsUrl`。

15. 【creator-builder/docs/contracts/20-step1-import.md:477-493】`import_pairings.pairing_code` 明文存储且全局唯一，6 位码会泄露且不可复用。  
【可操作修法】改存 `pairing_code_hash`，唯一约束限定 active pairing，补过期清理和尝试次数限制。

16. 【creator-builder/docs/contracts/50-step5-publish.md:426-463】【creator-builder/docs/01-详细技术方案.md:166】`publications.url_slug ≡ capabilities.slug` 没有 schema 保证，`marketplace_listings.slug` 也没有唯一约束，公开路径可能冲突或漂移。  
【可操作修法】去掉冗余 slug 改 join 读取，或用 trigger/generated column 保持一致，并给 listing slug 加唯一约束。

17. 【creator-builder/docs/contracts/10-auth-logto.md:291-295】【creator-builder/docs/contracts/60-dashboard-profile.md:548-550】社交写接口权限冲突：Auth 说 follow/like 要 creator role，Profile 说任意登录用户可操作。  
【可操作修法】按产品口径统一为 authenticated user 或 creator-only，并同步所有 endpoint 的鉴权表、错误用例和前端权限态。

VERDICT: FAIL
A) r3 两项未通过。

[P1] [pairings-repo.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/import/pairings-repo.ts:351) `createImportJobForPairing` 的 active predicate 已补全，但同一条 PG data-modifying CTE 先 `UPDATE import_pairings`，再 `link_job` 二次 `UPDATE` 同一行。真实 PostgreSQL 下同语句二次改同一行结果不可靠，可能创建 job 但 `job_id` 未落回 pairing，导致 `job_created` 无法恢复 `eventsUrl/jobId`。  
修法：改成 `active AS SELECT ... FOR UPDATE` 守门，`INSERT jobs ... FROM active`，最后单次 `UPDATE import_pairings SET phase='job_created', used_at=now(), job_id=new_job.id ...`。补真实 PG 语义回归：禁止 `phase='job_created' AND job_id IS NULL`，覆盖 expired/used/attempts。

[P1] [import.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/jobs/handlers/import.ts:380) `supersedePriorSnapshots` 已移入完成事务且有 fence/status guard，但 `completeJobInTx` 返回 `false` 时只是 `return false`；[db-tx.ts](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/events/db-tx.ts:35) 会提交非 throw 返回值，不会回滚。若 supersede 后、complete 前发生 cancel/fence-out，旧快照血缘仍会被提交污染。  
修法：`!completed` 必须抛 sentinel 触发 rollback 并在外层吞掉；同事务内先锁 job 行或给 guard 加 `FOR UPDATE`，保证 supersede 与 complete 共享同一个已锁定的 running/fence 命中状态。补交错回归：supersede 后 complete 前 fence/status 失效时，旧快照 `superseded_by` 保持不变。

B) STEP① 导入不能作为基线。未发现 P0，但以上两个 P1 仍阻塞配对建 job 的可恢复性，以及完成事务中取消/fence-out 不污染血缘的保证。

VERDICT: FAIL
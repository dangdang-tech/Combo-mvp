VERDICT: FAIL

[P0] (confidence: 9/10) [structure-repo.ts:113](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/structure/structure-repo.ts:113), [171](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/structure/structure-repo.ts:171), [237](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/structure/structure-repo.ts:237), [327](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/structure/structure-repo.ts:327) — `tgt` 普通 CTE 先读 `capability_versions`，但没有 `FOR UPDATE`。最终 `UPDATE` 等同一行并发锁后，会继续用 stale 的 `idx/next_state`。结果是迟到 `stuck/item/failed/done` 仍可能覆盖已经 `done/failed` 的当前行。`writeFieldDoneSurgical` 更严重，[structure-repo.ts:404](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/structure/structure-repo.ts:404) 用 `next_state.next` 整体写 `structure_state`，而 `next_state` 从 stale `tgt.structure_state` 重建，能擦掉并发 PATCH 或其它字段完成写。

修法：四个 `tgt` 都必须锁行读最新，再从锁后的 `tgt.structure_state` 算 `idx/next_state`。建议形态：

```sql
WITH tgt AS MATERIALIZED (
  SELECT id, manifest, structure_state
    FROM capability_versions
   WHERE id = $3
   FOR UPDATE
),
idx AS (...)
UPDATE capability_versions v
   SET ...
  FROM jobs j, tgt, idx
 WHERE j.id = $1
   AND j.fence_token = $2
   AND j.status = 'running'
   AND v.id = tgt.id
   AND v.id = $3
```

如果要把 fence 也做到并发取消后严格 0 行，用 `job_guard AS MATERIALIZED (SELECT id FROM jobs ... FOR UPDATE)`，再让 `tgt` 从 `job_guard` CROSS JOIN 进入，避免 wrong fence writer 先锁 version。

静态项我核过：没有 `jsonb_array_elements(v.structure_state)` / `FROM jobs j, LATERAL` 残留；四个最终 `UPDATE` 的 fence 条件还在；没看到新增 `any` 或 ErrorEnvelope `code`。但当前测试 [structure-repo-sql.test.ts:84](/Users/danielxing/repos/agora-mvp-creator-builder/creator-builder/apps/api/src/__tests__/structure-repo-sql.test.ts:84) 只守住“合法 LATERAL”，守不住这个 stale CTE 并发回归。加一个真 PG 交错测试：T1 `FOR UPDATE` 锁 version 并把字段置 `done` 后延迟提交，T2 调 `writeFieldStuckIfGenerating`，T1 commit 后 T2 必须返回 `false` 且字段保持 `done`。
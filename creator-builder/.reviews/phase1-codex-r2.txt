复审结论：FAIL。`_consistency-report.md` 的通过结论不成立。

17 项复核：#1 已修复；#2 已修复；#3 部分；#4 已修复；#5 部分；#6 已修复；#7 已修复；#8 未修复；#9 已修复；#10 已修复；#11 部分；#12 已修复；#13 已修复；#14 已修复；#15 部分；#16 已修复；#17 已修复。

阻塞项：

1. `creator-builder/docs/contracts/20-step1-import.md:44`、`:246` 仍把 import SSE 鉴权写成 `Bearer`，与 Logto 同源 Cookie 决策、`00` §11.C、`_index.md` 全局约定冲突。修法：该 endpoint 表和 4.1 统一改为 same-origin Cookie only，鉴权失败必须在建流前返回 HTTP ErrorEnvelope。

2. `creator-builder/docs/contracts/50-step5-publish.md:37` 声明 `review_rejected` 终态，但 `:56-57` 又允许 `review_rejected` 被同一发布事务直接置为 `published`。拒绝态真源仍不单一。修法：publish 只接受 `draft`；被拒重发必须先从 rejected version 派生新 draft version，原 rejected version 保留 `reject_reason/rejected_at/status` 不可变。同步修正 `:624` 对 `currentVersionId` “恒为 published态”的错误注释。

3. `creator-builder/docs/contracts/50-step5-publish.md:553-556` 只口头要求 publish batch item/counter 写入用 fence CTE，没有给出契约级 SQL 模板，不能证明“各 job 写入都用受保护写入”。修法：补充 `publish_batch_items` 更新与 `publish_batches` 计数更新的单语句 CTE，必须经 `publish_batches.job_id -> jobs` 校验 `j.id=:jobId AND j.fence_token=:fence AND j.status='running'`。

4. `creator-builder/docs/contracts/00-约定与状态机.md:120-133`、`:501-509` 仍把 ErrorEnvelope schema 定义为 `message`，而 §11.B 和各业务域要求 `userMessage` 且 UI 不渲染内部 code。错误契约仍分裂。修法：共享 TS 类型、示例、所有 schema 统一为 `userMessage`，删除 `message` 同义字段说法。

5. `creator-builder/docs/contracts/20-step1-import.md:166`、`:533` pairing 已哈希化，但失败尝试计数在“只提交 code”的 miss 场景下没有可更新的 pairing 行，`attempt_count += 1` 无法成立。修法：上传鉴权绑定 `pairId + code`，错误 code 也按 pairId 计数并限流，或补充独立 attempts/rate-limit 表。

6. `creator-builder/docs/contracts/70-events-infra.md:668-672` `runtime_sessions` 同时持有 `capability_id/version_id`，但只建了单列 FK，不能证明 version 属于该 capability。修法：改为复合 FK `(capability_id, version_id) -> capability_versions(capability_id, id)`，保留 `capability_versions` 的父级唯一键。

VERDICT: FAIL
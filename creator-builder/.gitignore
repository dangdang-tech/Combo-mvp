# Codex 对抗评审产物（日志 / 结论 / 证据）整目录不入库，不对外分享（本地保留作审计轨迹）
.reviews/

# 依赖/构建产物(脚手架阶段补充)
node_modules/
dist/
build/
.env
.env.local

# TypeScript 增量编译产物
*.tsbuildinfo
tsconfig.tsbuildinfo

# 测试 / 工具缓存
coverage/
.vitest/
.eslintcache

# 验收测试证据(截图 / 报告，体量大，不入库；本地保留作复测参考）
docs/测试/

# 编辑器
.DS_Store
*.log
